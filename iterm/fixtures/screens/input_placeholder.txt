# SYNTHETIC, not captured. Only ONE line here came from a live session: the
# input row "❯ Press up to edit queued messages", observed on 2026-08-10 in a
# session with queued messages. Everything around it was hand-written to frame
# it, and does NOT match any real capture: the rules are 96 characters where
# every genuine capture in this directory is 214, and only the footer line
# also appears in working_accept_edits.txt. Treat every row but the input row
# as invented. This branch exists because synthetic fixtures hid a real bug,
# so the header must not claim more provenance than the file has.
# Replace with a real capture when this state is next seen.
# state: working, box shows a PLACEHOLDER not a draft
⏺ Running 1 shell command…
  ⎿  $ echo hello
· Recombobulating… (13s · ↓ 393 tokens)
────────────────────────────────────────────────────────────────────────────────────────────────
❯ Press up to edit queued messages
────────────────────────────────────────────────────────────────────────────────────────────────
  ⏵⏵ accept edits on (shift+tab to cycle) · esc to interrupt · ← for agents
